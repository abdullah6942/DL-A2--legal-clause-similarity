# Legal Clause Similarity Detection - Google Colab Requirements
# Most packages come pre-installed in Colab, but listed here for reference

# Core Deep Learning (usually pre-installed in Colab)
torch>=2.0.0
torchvision>=0.15.0

# Data Processing (pre-installed in Colab)
numpy>=1.24.0
pandas>=2.0.0

# Machine Learning & Evaluation (pre-installed in Colab)
scikit-learn>=1.3.0

# Visualization (pre-installed in Colab)
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress tracking (pre-installed in Colab)
tqdm>=4.65.0

# Note: In Google Colab, you typically don't need to install these packages
# as they come pre-installed. However, if you need to install PyTorch with
# specific CUDA support, use:
# 
# !pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# For other packages, simply run:
# !pip install package_name

